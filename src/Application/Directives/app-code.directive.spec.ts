import { AppCodeDirective } from './app-code.directive';

describe('AppCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new AppCodeDirective();
    expect(directive).toBeTruthy();
  });
});
