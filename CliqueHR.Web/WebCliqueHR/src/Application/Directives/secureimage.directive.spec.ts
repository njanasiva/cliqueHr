import { SecureimageDirective } from './secureimage.directive';

describe('SecureimageDirective', () => {
  it('should create an instance', () => {
    const directive = new SecureimageDirective();
    expect(directive).toBeTruthy();
  });
});
