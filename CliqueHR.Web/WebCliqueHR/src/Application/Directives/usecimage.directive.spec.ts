import { UsecimageDirective } from './usecimage.directive';

describe('UsecimageDirective', () => {
  it('should create an instance', () => {
    const directive = new UsecimageDirective();
    expect(directive).toBeTruthy();
  });
});
